/* const likeContainers = document.querySelectorAll(".like-container");
const backgrounds = document.querySelectorAll(".background");

likeContainers.forEach(function (likeContainer, index) {
  likeContainer.addEventListener("click", function () {
    if (backgrounds[index].style.backgroundColor != "pink") {
      backgrounds[index].style.backgroundColor = "pink";
    } else {
      backgrounds[index].style.backgroundColor = "white";
    }
  });
});
*/